import { StyleSheet, Dimensions, windowWidth } from "react-native";

const styles = StyleSheet.create({
  size: {
    width: "100%",
    height: "20%"
  },
  container: {
    flexDirection: "row",
    flexWrap: "wrap"
  }
});

export default styles;
